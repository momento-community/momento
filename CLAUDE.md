# CLAUDE.md — Momentō

Project context for future Claude Code sessions. Keep lean.

## What this is

**Momentō** (always with the macron Ō) — a Pinterest-style local event discovery
app. Events are called **Momentos**. Domain: `momento.community`. Tagline: "Find
what's happening around you, right now."

## Locked identifiers

| Item | Value |
|---|---|
| App name | Momentō |
| Bundle / applicationId | `community.momento.app` (iOS + Android) |
| Domain | `momento.community` |
| Support email | `info@momento.community` |
| GitHub repo | `https://github.com/momento-community/momento.git` |
| Firebase project (single, dev+prod for now) | `momento-b23c0` |
| GCP / Firebase account | `info@momento.community` |
| Functions region | `europe-west1` |

A separate prod Firebase project will be split off before public launch.

## Tech stack (locked)

- Flutter 3.38+ (single codebase: iOS + Android + Web)
- Riverpod 2 (`flutter_riverpod`) for state
- `go_router` for navigation
- Firebase: Auth, Firestore, Storage, Cloud Functions (TS, 2nd gen)
- `google_maps_flutter` + `geoflutterfire_plus`
- `flutter_staggered_grid_view` for the masonry grid
- `cached_network_image`
- Stripe **not** installed yet — freemium gate is stubbed for v2

## Key files

- `lib/config/env.dart` — constants + `--dart-define` keys (Maps, Firebase project)
- `lib/config/theme.dart` — single source of truth for design tokens (colors, spacing, radii, shadows, text styles). **Never** inline hex outside this file.
- `lib/config/router.dart` — `go_router` with auth-gated redirects
- `lib/main.dart` / `lib/app.dart` — entry + `MaterialApp.router`
- `lib/firebase_options.dart` — generated by `flutterfire configure`
- `lib/core/widgets/momento_logo.dart` — `MomentoLogo` (Josefin 300, tracked) + `OBadge`
- `lib/core/widgets/momento_button.dart` — `@std.button` equivalent (`MomentoButton`)
- `lib/core/widgets/slide_up_route.dart` — slide-up route helper for detail pages
- `lib/features/auth/auth_repository.dart` — Firebase Auth + Google + Apple wrappers
- `lib/core/firebase/providers.dart` — Riverpod providers for Auth/Firestore/Storage + repositories + freemium-derived providers
- `lib/core/repositories/` — `MomentoRepository`, `UserRepository`, `StorageRepository`. All Firestore writes go through here.
- `lib/shared/filter_state.dart` — shared filter state for Discover + Map (Riverpod 3 `Notifier`)
- `firestore.rules` — strict ownership + freemium-quota rules (the load-bearing security gate now that we don't have a server-side callable)
- `firestore.indexes.json` — composite indexes for active feed, organizer feed, expiry scan, category+time
- `storage.rules` — only authed users upload their own images, ≤10MB, image content type
- `docs/design-export.md` — visual spec per screen (DslDocument summaries)
- `.github/workflows/deploy-hosting.yml`, `.github/workflows/deploy-rules.yml` — CI

## Brand & design rules

- Render the brand as **Momentō** with the macron everywhere (code, copy, UI). Events are always **Momentos** — no "events", no "flares".
- Logo wordmark uses `Ō` (U+014C) literal, never `O` with an overline.
- Lucide / Material outlined icons only. **No emoji. No gradients.**
- Cards: 16px radius, 1px `divider` border, `sm` shadow.
- Buttons: white-bg/charcoal-border default. **Max one** filled `primary` CTA per screen.
- Inputs: `surface` bg, 12px radius, no visible border unless focused.
- Test on iPhone 14 Pro frame (390×844) as the primary canvas. Web responsive: 1 col `<600px`, 2 cols `600–1200px`.

## Build status

- **Phase 1 (done)** — scaffold, theme, router with auth-gated redirects, splash + onboarding + auth (Google/Apple/Email).
- **Phase 2 (done)** — Discover masonry, Momento detail (slide-up), filter bottom sheet, shared filter state.
- **Phase 3 (done)** — Map placeholder + sage markers + compact card, Create form (image picker + freemium gate), My Moments (Organised + Liked tabs), Profile (avatar + stats + freemium card + tabs + logout), Organizer detail.
- **Phase 4 (done)** — `firestore.rules` + `firestore.indexes.json` + `storage.rules`, repository layer (`MomentoRepository`/`UserRepository`/`StorageRepository`), Firestore-backed providers replacing mock data, real photo upload from Create, freemium gate enforced via Firestore transaction, dev seed button on Profile, GitHub Actions for hosting + rules deploy.

## User roles

Three roles, stored as `users/{uid}.role`. Default `user` on signup.

| Role | What they can do |
|---|---|
| **user** | read + like + follow |
| **organisor** | + create / edit / delete own momentos + see analytics on own |
| **admin** | + edit / delete *any* momento + admin panel + manage roles + ban/unban users |

Self-service `user → organisor` via Profile or Create screen. Admin role is admin-only-grantable. Banned users keep read access but lose every write — enforced at the Firestore rule layer via `is_banned` checks on momento create/update/delete, follow create, and user self-update.

**Bootstrap:** the first admin doesn't exist until set manually — Firebase Console → Firestore → `users/{uid}` → set `role` = `"admin"`.

**Admin panel** lives at `/admin`, four tabs:
- **Momentos** — list every momento (newest first), with delete (audited).
- **Users** — list every user with inline role dropdown (audited) and a ban toggle (audited). Banned rows get a red ring + BANNED pill.
- **Audit log** — append-only stream of admin actions: actor, action, target, before/after diff, timestamp.
- **Stats** — counts of momentos / users (split by role) / total views / total likes.

**Audit log** lives in the `audit_log` Firestore collection. Append-only, admin-read-only. Action codes today: `momento.delete`, `user.role_change`, `user.ban`, `user.unban`. Schema in `lib/core/repositories/audit_log_repository.dart`.

Full plan: [docs/roles-plan.md](docs/roles-plan.md).

## Architecture decisions worth remembering

- **No Cloud Functions in v1.** The original spec called for a `createMomento` callable to enforce a 50m+time-overlap conflict check atomically. We dropped it because (a) the race window is vanishingly small in practice, (b) Firestore rules + a client-side transaction cover freemium / quota / ownership invariants, (c) Functions require Blaze + add cold-start latency. Re-add as a callable if duplicates ever surface.
- **`autoExpire` was also dropped.** Replaced by `where('end_datetime', '>', now())` in `MomentoRepository.watchActive` — expired Momentos are never read, no scheduled job needed.
- **`onMomentoLikeChange` dropped.** `liked_by` is the source of truth; `like_count` is kept in sync atomically inside `toggleLike`'s transaction.
- **Mock-data fallback.** Pass `--dart-define=USE_MOCK_DATA=true` to bypass Firestore and read the in-memory fixture from `lib/core/mock/mock_momentos.dart`. Default is false.

## Firebase Console / GCP — manual steps still required

- **Authentication → Sign-in method:** enable Google, Apple, Email/Password. ✅ done
- **Firestore Database:** created in `eur3 (europe-west)`. ✅ done
- **Storage:** default bucket created. ✅ done
- **Deploy rules + indexes** the first time: `firebase deploy --only firestore:rules,firestore:indexes,storage --project momento-b23c0`. CI does this on every push touching those files.
- **GitHub Actions secrets** for CI:
  - `FIREBASE_TOKEN` — generated locally via `firebase login:ci` (browser flow), pasted into GitHub → Settings → Secrets → Actions. We use this instead of a service-account JSON because the GCP org policy disables service-account-key creation.
  - `GOOGLE_MAPS_KEY_WEB` — when ready, restricted Maps JS API key for hosting domain.
- **Apple Sign-in (later):** configure Service ID + Key in Firebase Auth + Apple Developer Console + add Apple capability in Xcode signing.
- **Google Maps (later):** create + restrict 3 keys (iOS, Android, Web). Pass via `--dart-define=GOOGLE_MAPS_KEY_*=...`. Web also needs the Maps JS `<script>` in `web/index.html`.

## Common commands

```bash
# Run the app
flutter run -d chrome                                          # web — fastest dev loop
flutter run -d chrome --dart-define=USE_MOCK_DATA=true         # bypass Firestore (offline UI dev)

# Static analysis
flutter analyze

# Re-link Firebase (regenerate firebase_options.dart) — full path because PATH may not include pub bin:
"$LOCALAPPDATA/Pub/Cache/bin/flutterfire.bat" configure --project=momento-b23c0 \
  --platforms=android,ios,web \
  --android-package-name=community.momento.app \
  --ios-bundle-id=community.momento.app --yes

# Deploy (manual; CI does this on push to main):
firebase deploy --only hosting --project momento-b23c0
firebase deploy --only firestore:rules,firestore:indexes,storage --project momento-b23c0

# Seed dev Firestore: open the running app, sign in, go to Profile, hit
# "Seed dev Momentos" (debug-only button at the bottom of Profile).

# Mobile builds (manual)
flutter build ipa               # iOS
flutter build appbundle         # Android
```

## Development rules

- Update this file on every architectural, branding, or infra change.
- Keep widgets under ~300 lines; extract reusable bits into `lib/core/widgets/`.
- All colors and text styles via theme tokens — no inline hex codes outside `theme.dart`.
- Don't add Stripe, Instagram OAuth, push notifications, RSVPs, comments, or analytics in v1 — they're explicitly v2.
- Any "Contact" / footer email link must use `info@momento.community`.
- Never invent project IDs, API keys, or secrets. Ask once and write them to `env.dart` (via `--dart-define`).
